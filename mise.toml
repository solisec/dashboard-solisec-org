[tools]
gitleaks = "8.30.1"
node = "25.9.0"
pnpm = "10.33.0"
viteplus = "0.1.15"
wrangler = "4.80.0"

[tasks.secrets]
description = "Scan project for secrets"
run = "gitleaks dir ."

[tasks.check]
description = "Run project checks"
depends = ["secrets"]
