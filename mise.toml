[tools]
gitleaks = "8.30.1"
node = "25.9.0"
pnpm = "10.33.0"
viteplus = "0.1.15"
wrangler = "4.80.0"

[tasks.gitleaks]
description = "Scan the repository for secrets"
run = "gitleaks dir ."

[tasks.pre-commit]
description = "Run pre-commit checks"
depends = ["gitleaks"]
